import { ImageURISource } from 'react-native';

export class Icons {
  static readonly collectRewardIcon: ImageURISource = require('./icons/collectReward.png');
}

export class PlaceHolders {
  static readonly helloAgain: ImageURISource = require('./placeholders/helloAgain.jpeg');
}
